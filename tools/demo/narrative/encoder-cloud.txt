# NARRATIVE — encoder-cloud
#
# This file is TRACKED IN GIT. It is the script; the recording supplies the
# timings. Edit here, commit, and `git diff` shows what changed about what is
# SAID rather than what changed about when it was said.
#
#     python3 edit_text.py load     apply this into cues.json
#     python3 edit_text.py save     write cues.json back out to here
#
# The [NN] index is how a line is matched back — do not renumber or reorder.
# There are deliberately no timestamps here; see the module docstring.
# Blank lines and # comments are ignored. One cue per line.
#
# Text is expanded for the voice automatically (4K -> "four K"), so write it
# as you want it READ.

[00] Picking the same clip, this time for a cloud encode.
[01] Target: cloud. The identical job goes to AWS Batch instead of the machines on this network.
[02] Codec: H.264 only. Every codec becomes its own job, so dropping HEVC halves the work.
[03] Same ladder as before: apple-uniq-live-xs.
[04] On a cloud target the estimate is money: spot compute plus the egress to bring the media home.
[05] An output suffix gives this run its own directory, so the existing encodes of this source are untouched.
[06] Re-encode produces every variant fresh.
[07] The job is now a Step Functions execution. Every chunk becomes an AWS Batch job, launched on spot capacity.
[08] Waiting for compute resources. Batch is launching spot instances and pulling the image before a single frame is encoded.
[09] The cloud fleet: how many spot instances Batch has launched, and what they are costing.
[10] Per-instance packing and lifecycle, read from the AWS inventory.
[11] And the job, expanded. One row per chunk, per rung, as Batch works through the fan-out.
[12] Machines are up. Every chunk is now an independent Batch job, spread across as many instances as Batch will give us.
[13] The encode has finished, but the instances are still up — idle, and still billing, until Batch scales them down.
[14] The run reports what it actually cost, against what the same ladder would cost elsewhere — and how much of the machine time nobody was using.
[15] The chunks were encoded across 7 Batch instances.
[16] The encode is complete. The Outputs tab lists what landed in OUTPUT_DIR.
[17] The package opens into three areas.
[18] First, the ladder that was encoded. Its profile and VBV settings, then every rung with its resolution and bitrate.
[19] Second, the phase table. Every phase with its span, the CPU it consumed, and its peak memory.
[20] Third, the chunk timeline. One lane per machine, coloured by rung, so you can see which box encoded what.
